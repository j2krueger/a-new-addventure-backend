'use strict';


/**
 * Get the entry chain ending at the specified entry.
 * Get the entry chain starting with the first entry of the story and ending at the specified entry. Show the Story title, then for each entry show the entry title, the author, the keywords, the story text, and the choice text for the next entry in the chain if applicable.
 *
 * entryID  the ID of the entry
 * no response value expected for this operation
 **/
exports.get_chain = function(entryID) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

